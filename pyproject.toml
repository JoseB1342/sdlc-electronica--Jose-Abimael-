[tool.pytest.ini_options]
addopts = "--cov=semana2 --cov-report=term-missing --cov -fail-under=80"

[tool.ruff.lint]
select = ["E","F","I","UP","B"]

[tool.mypy]
disallow_untyped_defs = true
ignore_missing_imports = true
explicit_package_bases =true